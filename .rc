# TODO
# - add fish support
# - add completion support
# - add manpage support

if [[ -n $ZSH_VERSION ]]; then
  source "$(cd "$(dirname "$0")"; pwd)"/.rc.zsh
elif [[ -n $BASH_VERSION ]]; then
  source "$(cd "$(dirname "$BASH_SOURCE")"; pwd)"/.rc.bash
fi
